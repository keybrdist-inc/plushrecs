# yaml-language-server: $schema=.constellation/project.schema.json
config_version: 2
project:
  id: plushrecs
  name: Plush Recordings (label site — GZ orphan, active; distinct from plushrecs-agent)
  family: studio
git_host:
  provider: github
  slug: keybrdist-inc/plushrecs
tracker:
  adapter: github
  project_key: keybrdist-inc/plushrecs
loops: []
ci:
  provider: github-actions
testing:
  default: python3 -m unittest discover -s tests -v
  full_suite: python3 -m unittest discover -s tests -v
